{
  "name": "gemini",
  "version": "0.0.1",
  "description": "An cross platform application for application monitoring alerts",
  "scripts": {
    "api": "cd api && pnpm ",
    "client": "cd client && pnpm ",
    "dev:db:down": "docker-compose down",
    "dev:db:reset": "pnpm dev:db:down && pnpm dev:db:up && pnpm api migration:latest",
    "dev:db:up": "docker-compose up -d"
  },
  "author": "Cameron Adams <cameron.adams@hey.com>"
}
